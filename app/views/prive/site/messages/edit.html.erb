<h1>Modifier le message</h1>

<%= form_for [:prive, :site, @message] do |f| %>
	<%= render "/shared/error_messages", :target => @message %>	
	<%= render 'form', :f => f %>	
	<div>
		<%= f.submit 'Modifier', :class => 'btn' %>
		<%= link_to 'annuler', prive_site_messages_path %>
	</div>
<% end %>