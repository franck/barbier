<h1>Messages</h1>

<p>Un message est une information temporaire que vous désirez mettre en avant sur le site web.</p>

<p><strong>Example : </strong>"Toutes les crèmes solaire homme à -20% pendant tout le mois juin."</p>

<p>
	<%= link_to 'Nouveau message', new_prive_site_message_path, :class => 'btn' %>
</p>

<table class="table table-striped">
	<thead>
		<tr>
			<th>Titre</th>
			<th>&nbsp;</th>
			<th>&nbsp;</th>
		</tr>
	</thead>
	<% if @messages.size > 0 %>
	<% for message in @messages %>
	<tr>
		<td><%= message.title %></td>
		<td><%= message_state(message) %></td>
		<td>
			<%= message_publish_link(message) %>
			<%= link_to 'modifier', edit_prive_site_message_path(message) %>
			<%= link_to 'supprimer', prive_site_message_path(message), :method => :delete %>
		</td>
	</tr>
	<% end %>	
	<% else %>
	<tr>
		<td colspan="2">Aucun message</td>
	</tr>
	<% end %>
</table>
