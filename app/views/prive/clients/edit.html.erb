<h1>Modifier le client</h1>

<%= form_for [:prive, @client] do |f| %>

<%= render "form", :f => f %>

<div style="clear:both">
	<%= f.submit "Modifier", :class => 'btn' %>
	<%= link_to "annuler", prive_client_path(@client) %>
</div>
	
<% end %>