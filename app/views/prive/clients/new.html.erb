<h1>Nouveau client</h1>

<%= form_for [:prive, @client], :class => 'form-horizontal' do |f| %>

<%= render "form", :f => f %>

<div style="clear:both">
	<%= f.submit "Ajouter", class: 'btn' %>
	<%= link_to "annuler", prive_clients_path %>
</div>
	
<% end %>