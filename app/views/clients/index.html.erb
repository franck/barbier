<h1>Liste de clients</h1>

<%= link_to "Nouveau client", new_client_path, :class => 'second_button new_client_button' %>

<%= render "search" %>

<table>
	<tr>
		<th>Nom</th>
		<th>Email</th>
		<th>Tél</th>
	</tr>
	<% for client in @clients %>
	<tr>
		<td><%= client.name %></td>
		<td><%= client.email %></td>
		<td><%= client.phone %></td>
	</tr>
	<% end %>
</table>