<h1>Modifier salon</h1>

<%= form_for [:admin, @salon] do |f| %>
<%= render "form", :f => f %>
<div>
	<%= f.submit "Modifier", :class => 'btn' %>
	<%= link_to 'annuler', admin_salons_path %>
</div>
<% end %>